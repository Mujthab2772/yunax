import swaggerJsdoc from 'swagger-jsdoc';

const options: swaggerJsdoc.Options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'Modular E-commerce API',
      version: '1.0.0',
      description: 'API documentation for the enterprise e-commerce backend.',
    },
    servers: [
      {
        url: 'http://localhost:5000',
        description: 'Development Server',
      },
    ],
    components: {
      securitySchemes: {
        bearerAuth: {
          type: 'http',
          scheme: 'bearer',
          bearerFormat: 'JWT',
        },
      },
    },
    security: [
      {
        bearerAuth: [],
      },
    ],
  },
  // Document all routes
  apis: ['./src/modules/**/presentation/http/*.ts'], 
};

export const swaggerSpec = swaggerJsdoc(options);
